# CLAUDE.md — AI Game Studio

Guía de contexto para asistentes de IA que trabajen en este repositorio. Este proyecto se desarrolla con metodología **AI First**: la IA participa en diseño, código, documentación, pruebas y revisión.

## Qué es este proyecto

Plataforma **AI-First open source** para crear videojuegos 2D combinando el paradigma visual de Adobe Flash (timeline, fotogramas) con tecnología moderna (Rust, ECS, WGPU, Tauri) e IA nativa. Lema: *Build Games at the Speed of Imagination*. Principio central: **la IA conoce el videojuego y el videojuego conoce la IA** — todo el proyecto de juego vive en el formato `.aigs` (JSON AI-Ready) que editor, runtime, exportadores e IA comparten como contrato.

## Estado actual

- **Fase 2 (Motor completo) completada** — release **0.2.0** (2026-07-06), hitos M7–M13 en `docs/plan.md`. M13 cambió de alcance a petición del usuario: ya no es la demo de plataformas original, sino **persistencia de scripts entre partidas + demo Tamagotchi**, que reemplaza por completo al platformer. Bezier queda diferido sin fecha.
- **Fase 3 (Multiplataforma) cerrada sobre Desktop/Web/Android — release 0.3.0** (2026-07-06). M14 (Web/WASM) y M15 (Android) implementados, **validación manual en navegador/dispositivo real todavía pendiente** (ninguno disponible en este entorno de desarrollo). **M16 (iOS) diferido sin fecha** (decisión del usuario): necesita macOS + Xcode, no disponibles aquí; se retomará cuando haya máquina. M17 completó: menú de exportación Desktop/Web/Android en el editor, CI de los tres exportadores contra los ejemplos, presupuesto de tamaño (`strip`/`wasm-opt`), guía de publicación (`docs/guia-publicacion.md`). Viabilidad de `wasm32-unknown-unknown` y `aarch64-linux-android` confirmada por compilación **y enlace** reales antes de comprometer el diseño: `aigs-render`/`aigs-runtime` (wgpu+winit, rapier2d, rhai, kira) compilan para ambos targets; wasm necesitó resolver el bloqueo de `getrandom` (feature `wasm_js`), Android necesitó instalar de verdad NDK r28c + SDK (build-tools 34, platform 34) + `cargo-apk` 0.10.0. Trait `AssetSource` (`runtime/crates/aigs-runtime/src/source.rs`) abstrae la carga de bytes: Desktop = `PathBuf`/disco, Web = `MemoryAssets`/`fetch` prellenado (asíncrono), Android = `AndroidAssets`/`AAssetManager` (síncrono, como Desktop, porque los assets ya están dentro del APK). Hot reload de scripts queda inerte en Web/Android sin código especial (`as_path()` devuelve `None`).
- **Fase 4 (IA profunda) en curso — M18 (AI Core y chat) completado** (2026-07-06). `editor/src-tauri/src/ai.rs`: `Provider` como `enum` Ollama/Claude (sin `dyn Trait`/`async-trait`, un `match` en una `fn chat` async basta con 2-3 variantes), selección por variables de entorno (`AIGS_AI_PROVIDER`, sin panel de ajustes todavía), comando Tauri `ai_chat`. El contexto lo construye el **frontend** (`ChatPanel.tsx`), no el backend — serializa el proyecto/escena tal como están en memoria del editor (incluye cambios sin guardar), límite ~12000 caracteres. Panel de Chat nuevo junto a Timeline/Consola, solo lectura (escritura asistida es M19). Verificado con una instancia real de **Ollama** local (`llama3.2`, test de integración que se salta si no hay servidor escuchando); proveedor **Claude** implementado contra la Messages API pública pero **sin verificar en vivo** (necesita `ANTHROPIC_API_KEY` de un usuario, nunca solicitada por el chat). `editor/src-tauri` estaba excluido del workspace raíz y por tanto nunca cubierto por el `cargo fmt --all --check` de CI — al correr `cargo fmt` por primera vez salió a la luz deriva de formato preexistente en `lib.rs` (corregida mecánicamente). Build (`npm run build`) y arranque (`npm run tauri dev`) verificados; el recorrido de clics en la UI real **no** se pudo verificar (sin automatización de pantalla en este entorno).
- **M19 (escritura asistida) completado** (2026-07-06). Segundo modo de Chat, **"Proponer cambios"**, elegido por el usuario (no inferido por el modelo). El modelo responde con un único JSON (`ChangeProposal`: altas/parches/bajas de entidades + scripts `.rhai` nuevos, `format:"json"` de Ollama + extracción tolerante del primer `{...}` para el resto), validado en Rust contra los tipos reales del formato (`aigs_project::EntityNode`/`Components`) — asset inventado, componente mal tipado, id de entidad duplicado/desconocido o script que no compila (`rhai::Engine::compile` de verdad) se rechazan ahí mismo, todo o nada. Aplicar reutiliza `UPDATE_DOCUMENT`/undo ya existente desde M3 — deshacer un cambio de la IA es `Ctrl+Z`. **Verificado de punta a punta con Ollama real** (`qwen2.5-coder:7b`, ~7 min en CPU): "añade una moneda en (50,50) usando el sprite existente" produjo una propuesta válida sin inventar nada, conservado como test `#[ignore]` (no en el suite normal por su duración). 9 tests unitarios más (sin red) cubren los casos de rechazo. `cargo fmt`/`clippy`/`test` limpios en `editor/src-tauri` y en el workspace principal; build/arranque del editor verificados, recorrido de clics en la UI real no verificado (sin automatización de pantalla aquí).
- **M20 (agentes especializados) completado** (2026-07-07). Tercer modo de Chat, **"Orquestar agentes"**: nuevo módulo `editor/src-tauri/src/agents.rs`, un Arquitecto planifica pasos (`{"summary","steps":[{"agent","instruction"}]}`, máx. 8) con el mismo mecanismo JSON de M19; cada paso corre contra el *prompt* de su especialista y se valida con la misma `parse_and_validate_proposal`, ahora parametrizada por un `AgentKind` opcional que impone **lista blanca de componentes por agente** (comprobada en Rust): Arquitecto (`transform2d`/`sprite`), Diseñador de niveles (+`collider2d`), Programador (`script`/`behaviors`), Física (`rigidbody2d`/`collider2d`/gravedad), Audio (`behaviors`/música), Animador (`animator`, solo animaciones ya existentes — sin autoría de keyframes, eso sigue siendo del Timeline). **"Optimización" diferido**: no hay datos de perfilado (tamaño de assets, FPS reales) que lleguen al modelo todavía, así que un agente de "optimización" no tendría base real sobre la que proponer nada. Nuevo campo `scene_patch: {gravity?, music?}` en `ChangeProposal` para que Física/Audio toquen campos de la escena (M19 solo cubría entidades). Pasos secuenciales, acumulando ids/assets/animaciones para que un paso posterior referencie lo creado por uno anterior; todo el plan se rechaza si cualquier paso falla, señalando cuál. El resultado se fusiona en el mismo `ChangeProposal` de M19, así que la tarjeta de propuesta y el aplicar/deshacer del editor no cambiaron. **Verificado de punta a punta con Ollama real**: una primera ejecución expuso la validación funcionando de verdad (el paso de Diseñador de niveles propuso `"shape":"rectangle"`, que el formato no admite — solo `"box"`/`"circle"` — y la orquestación se rechazó señalando el paso y el motivo); se ajustó el *prompt* para deletrear los valores exactos de esos enums, y una segunda ejecución confirmó un plan de dos pasos generado, validado y fusionado correctamente. `cargo fmt`/`clippy`/`test` limpios; build/arranque del editor verificados, recorrido de clics en la UI real no verificado.
- **M21 (generación de juegos completos) completado — FASE 4 (IA profunda) CERRADA** (2026-07-07). Cuarto modo de Chat, **"Generar juego"**: nuevo rol **"Productor"** en `agents.rs`, un nivel de planificación por encima del Arquitecto que decide qué **escenas** hace falta (la ya abierta y/o nuevas, máx. 6), cada una resuelta reutilizando el **mismo** `orchestrate()` de M20 sin ningún cambio. Cierra una brecha real de M20 (su propio ejemplo, "crea un segundo nivel", necesitaba una escena nueva que `orchestrate()` nunca soportó solo). El resultado (`ProjectProposal`: un `ChangeProposal` por escena) se aplica de forma **atómica** — un único `UPDATE_DOCUMENT` para todas las escenas, un solo `Ctrl+Z` para deshacer el juego completo. La iteración conversacional no necesitó código nuevo (verificado por inspección: el chat reconstruye su contexto desde el estado en memoria en cada envío). **Límite honesto de alcance:** "juego completo" es estructura completa (escenas, entidades, física, comportamientos, scripts), no arte/audio nuevo — sin modelo de generación de imágenes/sonido integrado; si falta un asset, ese paso falla la validación igual que en M19/M20. **Verificado de punta a punta con Ollama real** (`qwen2.5-coder:7b`), con un caso de estudio real (menú con el sprite del héroe centrado + nivel con una plataforma estática, mismo sprite): **hicieron falta cinco intentos reales** (5-14 min cada uno en CPU) hasta llegar a un resultado válido. Los cuatro primeros expusieron, cada uno, un fallo de validación genuino y distinto — un campo de texto obligatorio en `null`, un script anidado como objeto en vez de referenciado por id, y dos veces un especialista intentando recrear con `entities_to_add` una entidad que otro paso del mismo plan ya había creado en vez de usar `entities_to_update` — y la validación rechazó cada uno correctamente, señalando el paso y el motivo exactos, nunca aplicando nada parcial. Cada hallazgo se corrigió aclarando el *prompt* compartido (beneficia también a M19/M20). El quinto intento generó, validó y fusionó las dos escenas correctamente. `cargo fmt`/`clippy`/`test` limpios; build/arranque del editor verificados, recorrido de clics en la UI real no verificado. Con M21 se cierran los cuatro hitos de la Fase 4 (M18-M21); release **0.4.0** publicado siguiendo el mismo procedimiento de 0.1/0.2/0.3.
  - **M14 (Web):** arranque del renderer reescrito como máquina de estados porque `pollster::block_on` bloquearía el único hilo de JS del navegador (`Renderer::new_async` + `Rc<RefCell<Option<...>>>`, resuelto perezosamente); Desktop no cambia de comportamiento. Nuevos crates `exporters/web-player` (`aigs-web-player`, cdylib wasm32) y `exporters/web` (`aigs-export-web`). `aigs export --target web` busca el jugador prebuilt en `<carpeta del ejecutable>/web-player/` (construido una vez con `wasm-bindgen`) y genera `index.html` + `data/` — mismo self-player que M7. Verificado de punta a punta (build wasm32 limpio, módulo JS válido, export de Robot Rescue con la estructura esperada); **no** verificado dentro de un navegador real. Sin `save.json` en Web todavía.
  - **M15 (Android):** Android sí puede bloquear su hilo (SO real, no JS de navegador), así que `run_android()` reutiliza el mismo bucle bloqueante de Desktop, solo cambia cómo se construye el `EventLoop` (`with_android_app`). `suspended()`/`resumed()` nuevos: tiran y reconstruyen `window`/`renderer` al pasar a segundo plano/reanudar (limitación conocida: las texturas no se recargan tras un resume, sin crash gracias a que el renderer indexa con `.get()`). Entrada táctil (`WindowEvent::Touch`) alimenta el mismo `Input` que el ratón. Nuevo componente de formato `virtual_button: { key }` simula una tecla mientras se toca su sprite (necesario para que Robot Rescue sea jugable por touch). A diferencia de Web, **no hay jugador genérico reutilizable**: Android empaqueta los assets dentro del APK en tiempo de build, así que `exporters/android-player` es una plantilla que cada export copia/retoca (assets + `package`/`apk_name` únicos) y compila de verdad con `cargo apk build` (`exporters/android`, `aigs-export-android`) — requiere NDK/SDK/`cargo-apk` en la máquina que exporta, documentado como requisito externo (igual que Xcode para iOS). Verificado de punta a punta: Robot Rescue exportado a un `.apk` firmado real, arm64-v8a, confirmado con `aapt dump badging`; **no** instalado ni jugado en un dispositivo/emulador real. Sin `save.json` en Android tampoco.
  - **M17 en curso:** el botón **⬇ Exportar** del editor ya tiene un desplegable Desktop/Web/Android (`editor/src/panels/Toolbar.tsx`); `export_project` (`editor/src-tauri/src/lib.rs`) recibe `target` y lo reenvía como `--target` al CLI (sin `--zip` fuera de Desktop). Los errores de Web/Android sin plantilla/toolchain llegan tal cual a la consola del editor. Sigue sin haber UI para firmar un release de Android (keystore propio) — eso sigue siendo solo CLI con `--release`.
- **M13 entregó (persistencia):** `save.json` fuera del formato `.aigs` (estado de partida, no dato de diseño) — `SaveData` en `aigs-runtime` (versión, timestamp, memoria de scripts por id de entidad), `ScriptHost.persisted_memory` sobrevive a `goto_scene` (clave = id de entidad autor, no el handle ECS que no es estable entre escenas), `elapsed_since_save()` con **semántica de consumo único** (el primer script que la llama en la sesión recibe el hueco real; el resto de llamadas devuelven 0), `set_scale(sx, sy)` nuevo (crecimiento por edad de la mascota). `aigs run` carga `save.json` al arrancar y autoguarda cada 600 ticks (~10 s); sin gancho de cierre limpio todavía. Demo: `examples/tamagotchi/` — mascota con hambre/felicidad/salud que decaen en tiempo real incluso con el juego cerrado, alimentar/jugar/medicina con teclas 1/2/3, todo en un único script (`pet.rhai`) sin más código.
- **Parche M12 (post-hito, a petición del usuario):** (1) **manifiesto tipado y máquina-legible** — `aigs_runtime::api_manifest()` describe cada función/lifecycle con nombre, parámetros tipados y descripción; snapshot en `sdk/aigs-format/scripting-api.json` (regenerar con `aigs script-api > sdk/aigs-format/scripting-api.json`), un test de integración lo mantiene sincronizado y un smoke test invoca cada función documentada para probar que existe con la aridad correcta. (2) **hot reload** — `ScriptHost` vigila el `mtime` de cada `.rhai` cada 30 ticks (~2×/s) y recompila sin reiniciar Play mode; error de compilación conserva la versión anterior. (3) **`on_collision(other)`/`on_destroy()`** además de `on_start`/`on_update` — corre tras el physics step (mismo tick que los behaviors de colisión) y justo antes de `world.clear()` en cada cambio de escena; `goto_scene` desde `on_destroy` se ignora con warning para no encadenar cambios de escena. (4) **bugfix de diseño importante:** las funciones `fn` de rhai NO ven el `Scope` entre llamadas (`this.campo` nunca persistía pese a la doc original) — sustituido por `get_var(nombre)`/`set_var(nombre, valor)`, memoria float explícita por instancia en Rust, mucho más predecible para que una IA genere el script correctamente. `patrol.rhai` de robot-rescue actualizado.
- M12 entregó: scripting con **rhai 1.25** sandboxeado (sin IO, 200k ops/tick, call levels 32) — asset kind `script` (.rhai) + componente `script: {asset}`; `ScriptHost` compila ASTs una vez y corre una instancia por entidad con `fn on_start()`/`fn on_update(dt)`; la API lee por snapshot (x/y/rotation/frame, x_of/y_of/distance_to, key_*) y muta por **cola de comandos** (set_pos/move_by/set_rotation/set_frame directos al mundo; goto_scene/play_animation/play_sound/emit_particles al GamePlayer); errores compilación/runtime → warnings y el script se desactiva (failed flag, sin spam). Corre tras behaviors/animators y antes de física (mueve kinemáticos). Editor: import .rhai (📜), sección Script en inspector. Demo: dron patrullero en robot-rescue (~15 líneas).
- M11 entregó: componente `particles` en el formato (tasa/vida/velocidad/dirección/apertura/gravedad/escala/opacidad/capa/emitting) + acción `emit_particles {count}` — las partículas son **entidades ECS** (`Particle` + `Transform2D` + `Sprite`) simuladas por `particles::tick` en GamePlayer (emisión continua por acumulador + ráfagas; PRNG xorshift por emisor sembrado con el id), así reutilizan el render instanciado/capas/interpolación gratis. Editor: sección Partículas en inspector con **vista previa en canvas en vivo** + acción en behaviors. Demos: chispas al tocar el sensor del playground, confeti en la victoria de robot-rescue.
- M10 entregó: spritesheets (`spritesheet: {frame_width, frame_height}` en el asset, `sprite.frame` animable con truncado a entero, UV rect por instancia en el shader), evento `key_released`, y **máquinas de estados de animación** (`animator: {initial, states, transitions}` — sus animaciones no se auto-reproducen; stop al salir del estado, restart al entrar; transiciones por teclas/scene_start/animation_end). Editor: frame en inspector/viewport/timeline, botón ▦ en Recursos para configurar la rejilla, sección Animator completa. Demo: robot de robot-rescue camina con spritesheet de 6 frames e idle↔walk por flechas. Recorte consciente: curvas bezier diferidas a M13.
- M9 entregó: audio con **kira 0.12** (features `cpal,wav,pcm` — ojo: `wav` solo trae el lector RIFF, sin `pcm` los WAV dan "unsupported codec"; volumen lineal→decibelios) — `AudioPlayer` en runtime (efectos `play_sound`, música por escena con `music: {asset, volume, looped}` que **continúa entre escenas si es el mismo asset**, modo deshabilitado sin dispositivo para CI/headless), acción `play_sound` en behaviors, editor con importación/pre-escucha de audio en Recursos y música en propiedades de escena, WAVs procedurales en robot-rescue (tema + pop + jingle de victoria) y physics-playground (bump en colisiones). CI/release con `libasound2-dev` en Ubuntu.
- M8 entregó: física 2D con **rapier2d 0.33** (¡API glam, no nalgebra!: `Pose::new(Vector::new(x,y), ang)`, `pipeline.step` de 12 args sin query pipeline) — componentes `rigidbody2d` (dynamic/kinematic/static, gravity_scale, velocidad inicial, fixed_rotation) y `collider2d` (box/circle, sensor, restitution, friction, tamaño default del sprite visible), `gravity` por escena (default 0,-980), `PhysicsWorld` a paso fijo (kinemáticos siguen al transform, dinámicos escriben de vuelta), evento `collision` (filtro `with`) integrado en GamePlayer, secciones de física en el inspector + gravedad en propiedades de escena (sin selección), ejemplo `examples/physics-playground` (60 FPS, 10 entidades). Limitación documentada: `move` sobre cuerpos dinámicos no altera la simulación.
- M7 entregó: `aigs export <game.aigs> [--output dir] [--zip]` con diseño **self-player** (el binario `aigs` ejecuta `data/game.aigs` si existe junto al ejecutable; exportar = copiar ejecutable renombrado + datos validados), crate `aigs-export-desktop` en `exporters/desktop` (valida antes de escribir, no sobrescribe, zip opcional), botón **⬇ Exportar** en el editor. Validado: Robot Rescue exportado a carpeta limpia corre standalone a 63 FPS.
- **Fase 1 (MVP) completada** — release **v0.1.0** (2026-07-03) con instaladores del editor y binarios del CLI para 3 SO.
- M6 entregó: benchmarks Criterion con baseline en `docs/testing.md` (query2 10k ≈ 21,5 µs), demo **Robot Rescue** (`examples/robot-rescue`: menú → nivel jugable → victoria, encadenado con `animation_end`, sprites procedurales, 100 % datos), workflow de release (`.github/workflows/release.yml`, tag `v*` → tauri-action + binarios CLI), `CHANGELOG.md`, guía de usuario `docs/guia-inicio.md` y README con estado 0.1.0.
- M5 entregó: componente `behaviors` en el formato (eventos `key_down`/`key_pressed`/`click`/`scene_start`/`animation_end` → acciones `move`/`goto_scene`/`play_animation`, documentado en SPEC), `GamePlayer` en runtime (multi-escena con `World::clear`, hit-test de clic con cámara, warnings de binding), `aigs run` reescrito sobre el player con stats por stdout (`stats: fps=… entities=…`), editor con gestión de escenas (crear/duplicar/eliminar/★ inicial en la toolbar), sección Comportamientos en el inspector (formulario "Cuando…hacer…"), y logs/métricas del player streameados a la consola del editor vía eventos Tauri (`player-log`/`player-err`). hello-world ahora es un juego: menú (clic/Enter → nivel) + nivel (flechas mueven, Esc vuelve, clic en el goal reproduce animación).
- M4 entregó: `aigs_anim::sample` (evaluación de pistas con easing; `Keyframe` unificado y reexportado por `aigs-project`), `aigs_runtime::AnimationPlayback` (bind escena→entidades con warnings, avance por tick, loop con wrap, hold del valor final), `aigs run` reproduce las animaciones de la escena, y en el editor: panel **Timeline** con pestañas inferiores (Timeline/Consola) — selector de animaciones, fps/loop, pistas por entidad+propiedad, keyframes arrastrables con editor de frame/valor/easing, doble clic para insertar, scrubbing y reproducción con preview en el viewport (`state.preview`, no undoable; viewport de solo lectura mientras hay preview). Espejo TS del muestreo en `editor/src/anim.ts` (mantener en sync con `aigs-anim`).
- M3 entregó: editor base — backend Tauri revalidando con `aigs-project`, undo/redo por snapshots, viewport Canvas 2D, árbol, inspector, recursos, consola, Play → `aigs run`.
- **Para ejecutar el editor:** `cd editor && npm run tauri dev` (`webkit2gtk-4.1` ya instalado en esta máquina). El botón Play usa el CLI `aigs`, ya instalado en `~/.cargo/bin` (`cargo install --path cli` para reinstalar tras cambios).
- Decisión clave M3: viewport de edición en Canvas 2D (misma matemática TRS que el runtime); el runtime WGPU real corre en Play vía `aigs run`. Ver tabla en `docs/arquitectura.md`.
- Deuda técnica: `wgpu` 24 / `winit` 0.30 (actualizar tras MVP); jerarquía aplanada al instanciar en runtime; docking fijo (CSS grid) sin paneles arrastrables; animación por spritesheet (frame de sprite) diferida — necesita extensión del formato (UV/frames), prevista con los spritesheets de Fase 2.
- Gotcha: en Wayland+EGL el `Renderer` debe soltarse en `exiting()` de winit o segfaultea (resuelto en `aigs-runtime/src/app.rs`).

## Mapa del repositorio

| Ruta | Contenido |
|---|---|
| `docs/` | Documentación de diseño (fuente de verdad del proyecto). |
| `editor/` | Editor visual — Tauri 2 + React + Vite + TypeScript (`npm run build` / `npm run tauri dev`). |
| `runtime/crates/` | Crates del motor: `aigs-ecs`, `aigs-render`, `aigs-anim`, `aigs-project`. |
| `cli/` | Binario `aigs`: `validate <game.aigs>` y `run <game.aigs>` (ejecuta la escena inicial; `AIGS_MAX_FRAMES` para smoke tests). |
| `exporters/desktop/` | `aigs-export-desktop`: exportación a carpeta auto-contenida (self-player). |
| `sdk/aigs-format/SPEC.md` | **Especificación normativa del formato `.aigs`** — mantener en sync con `aigs-project`. |
| `examples/hello-world/` | Proyecto `.aigs` mínimo válido; fixture del CI. |
| `tests/` | Tests de integración del sistema completo. *(vacío aún)* |

El workspace Cargo vive en la raíz (`Cargo.toml`); `editor/src-tauri` queda excluido y se compila aparte. Comandos: `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --all`.

## Mapa de documentación (leer antes de trabajar)

| Documento | Qué contiene | Léelo cuando… |
|---|---|---|
| [docs/proyecto.md](docs/proyecto.md) | Visión completa, filosofía, objetivos, público, principios | necesites contexto general |
| [docs/plan.md](docs/plan.md) | **Plan maestro**: hitos M0–M6 del MVP con tareas, entregables y riesgos | vayas a implementar cualquier cosa |
| [docs/arquitectura.md](docs/arquitectura.md) | Módulos, formato `.aigs`, IPC editor↔runtime, **tabla de decisiones** | tomes o cuestiones decisiones técnicas |
| [docs/editor.md](docs/editor.md) | Paneles y arquitectura interna del editor | trabajes en `editor/` |
| [docs/runtime.md](docs/runtime.md) | Crates, ECS, game loop, modos de ejecución | trabajes en `runtime/` |
| [docs/ia.md](docs/ia.md) | Estrategia IA, agentes, formato AI-Ready | trabajes en integración con IA |
| [docs/exportadores.md](docs/exportadores.md) | Plataformas y diseño de exportación | trabajes en `exporters/` |
| [docs/guia-publicacion.md](docs/guia-publicacion.md) | Pasos manuales para publicar en cada tienda (Web, Play Store) | vayas a publicar un juego exportado |
| [docs/plugins.md](docs/plugins.md) | SDK y extensibilidad | trabajes en `sdk/` |
| [docs/testing.md](docs/testing.md) | Estrategia de pruebas por nivel | escribas o revises tests |
| [docs/ci-cd.md](docs/ci-cd.md) | Pipeline, quality gates, releases | toques CI o publiques releases |
| [docs/vision.md](docs/vision.md) | Misión, visión y creencias | redactes comunicación del proyecto |

## Reglas de trabajo

1. **El plan manda:** todo trabajo se enmarca en un hito de [docs/plan.md](docs/plan.md); no adelantar alcance de fases futuras.
2. **Decisiones trazables:** toda decisión de arquitectura se registra en la tabla de [docs/arquitectura.md](docs/arquitectura.md) *antes* de implementarse.
3. **Docs sincronizadas:** si un cambio afecta a un documento de `docs/`, se actualiza en el mismo commit/PR.
4. **Formato `.aigs` es sagrado:** cualquier cambio exige actualizar la especificación en `sdk/` y las migraciones.
5. **Calidad:** Conventional Commits; `cargo fmt`/`clippy` y `prettier`/`eslint` limpios; ningún hito se cierra sin tests ([docs/testing.md](docs/testing.md)).
6. **Idioma:** documentación en español; código, identificadores y mensajes de commit en inglés.

## Trazabilidad (bitácora de hitos del desarrollo)

Registro cronológico de los pasos mayores del proyecto. Añadir una línea por cada avance significativo.

| Fecha | Avance | Referencia |
|---|---|---|
| 2026-07-07 | **M21 completado — generación de juegos completos, FASE 4 (IA profunda) CERRADA (M18-M21).** Cuarto modo de Chat, "Generar juego": un nuevo rol "Productor" planifica qué escenas hace falta (la ya abierta y/o nuevas), cada una resuelta reutilizando el mismo motor de orquestación de M20 sin cambios — cierra una brecha real que M20 había dejado abierta (crear una escena nueva). Aplicar un juego generado es un único commit de historial (todas las escenas a la vez). Límite honesto: genera estructura completa (escenas, entidades, física, comportamientos, scripts) reusando assets ya importados, no arte/audio nuevo. Verificado de punta a punta con Ollama real: de 5 intentos reales del caso de estudio (menú + nivel), los 4 primeros expusieron cada uno un fallo de validación genuino y distinto (rechazados correctamente, nunca aplicados parcialmente), corregidos aclarando el prompt compartido; el 5º generó/validó/fusionó las dos escenas correctamente. Release **0.4.0** publicado. | [editor/src-tauri/src/agents.rs](editor/src-tauri/src/agents.rs) |
| 2026-07-07 | **M20 completado — agentes especializados.** Tercer modo de Chat, "Orquestar agentes": un Arquitecto planifica pasos y cada uno corre contra un especialista (Arquitecto, Diseñador de niveles, Programador, Física, Audio, Animador) con **alcance de escritura por lista blanca de componentes comprobada en Rust**, no solo pedida en el prompt. "Optimización" queda diferido (sin datos de perfilado que lleguen al modelo todavía); "Animador" solo conecta animaciones ya existentes, sin autoría de keyframes. Nuevo módulo `editor/src-tauri/src/agents.rs`; nuevo campo `scene_patch` en `ChangeProposal` para que Física/Audio toquen gravedad/música de escena. Pasos secuenciales (acumulan ids/assets para que uno posterior referencie lo creado por uno anterior); todo o nada si cualquier paso falla. Verificado de punta a punta con Ollama real: una primera ejecución expuso la validación rechazando de verdad un `"shape":"rectangle"` inválido; ajustado el prompt y una segunda ejecución confirmó un plan de dos pasos generado/validado/fusionado correctamente. | [editor/src-tauri/src/agents.rs](editor/src-tauri/src/agents.rs), [editor/src-tauri/src/ai.rs](editor/src-tauri/src/ai.rs) |
| 2026-07-06 | **M19 completado — escritura asistida y primer agente "Programador".** El Chat gana el modo "Proponer cambios" (elegido por el usuario, no por el modelo): el modelo responde un único JSON (`ChangeProposal`) validado en Rust contra los tipos reales del formato (`aigs_project::EntityNode`/`Components`), con comprobación de referencias a assets, ids de entidad y compilación real de cada script `.rhai` nuevo (`rhai::Engine::compile`) antes de mostrarlo — todo o nada. Aplicar reutiliza `UPDATE_DOCUMENT`/undo ya existente desde M3. Verificado de punta a punta contra un Ollama real (`qwen2.5-coder:7b`): una instrucción en lenguaje natural produjo una propuesta válida sin inventar assets ni ids, conservado como test `#[ignore]` por su duración (~7 min). | [editor/src-tauri/src/ai.rs](editor/src-tauri/src/ai.rs), [editor/src/panels/ChatPanel.tsx](editor/src/panels/ChatPanel.tsx), [editor/src/document.ts](editor/src/document.ts) |
| 2026-07-06 | **M18 completado — AI Core y chat con contexto (Fase 4 en curso).** `Provider` como `enum` Ollama/Claude en `editor/src-tauri/src/ai.rs` (sin `dyn Trait`/`async-trait`), selección por variables de entorno, comando Tauri `ai_chat`. El contexto lo construye el **frontend** (`ChatPanel.tsx`) a partir del documento en memoria del editor (incluye cambios sin guardar), no el backend releyendo disco. Panel de Chat nuevo en el editor, solo lectura. Verificado con una instancia real de Ollama local (`llama3.2`); proveedor Claude implementado contra la Messages API pública pero sin verificar en vivo (necesita API key de un usuario). De paso: `cargo fmt` reveló y corrigió deriva de formato preexistente en `editor/src-tauri` (excluido del workspace raíz, nunca cubierto por CI hasta ahora). | [editor/src-tauri/src/ai.rs](editor/src-tauri/src/ai.rs), [editor/src/panels/ChatPanel.tsx](editor/src/panels/ChatPanel.tsx), [docs/ia.md](docs/ia.md) |
| 2026-07-06 | **M17 completado — FASE 3 (Multiplataforma) cerrada sobre Desktop/Web/Android, release v0.3.0.** M16 (iOS) diferido sin fecha por decisión del usuario (necesita macOS + Xcode). Menú de exportación Desktop/Web/Android en el editor (`Toolbar.tsx` + `export_project` con parámetro `target`); CI del exportador Web ampliada a los cuatro ejemplos (Android se queda solo con Robot Rescue — cada export ahí recompila el motor entero sin caché entre ejemplos); presupuesto de tamaño (`opt-level="s"` + `lto` + `strip` en ambos jugadores, `wasm-opt -Oz -all` sobre el `.wasm` en CI — gotcha real: `wasm-opt` rechaza por defecto el `bulk-memory`/`sign-ext` que emite rustc, hace falta `-all`); nueva guía `docs/guia-publicacion.md`. | [Release v0.3.0](https://github.com/Codigo-Free/ai-game-studio/releases/tag/v0.3.0), [docs/guia-publicacion.md](docs/guia-publicacion.md) |
| 2026-07-06 | **M15 (exportador Android) implementado.** `run_android()` + ciclo de vida `suspended`/`resumed`; entrada táctil (tap = click) reutilizando el `Input` existente; componente de formato nuevo `virtual_button: { key }` para controles estilo teclado en pantalla; `AndroidAssets` (lectura síncrona del APK vía `AAssetManager`); crates `exporters/android-player` (plantilla de build, no jugador único reutilizable — Android empaqueta assets en tiempo de build) y `exporters/android` (`aigs-export-android`, copia/retoca la plantilla y ejecuta `cargo apk build`); `aigs export --target android [--release]`. Verificado instalando de verdad NDK r28c + SDK (build-tools 34, platform 34) + `cargo-apk` 0.10.0: Robot Rescue exportado a un `.apk` firmado real, arm64-v8a, confirmado con `aapt dump badging`; **pendiente validación manual en un dispositivo/emulador real** (no disponible en este entorno). | [runtime/crates/aigs-runtime/src/source.rs](runtime/crates/aigs-runtime/src/source.rs), [exporters/android-player/](exporters/android-player/), [exporters/android/](exporters/android/) |
| 2026-07-06 | **M14 (exportador Web/WASM) implementado — FASE 3 iniciada.** Viabilidad de wasm32 verificada por compilación real; trait `AssetSource` (Desktop/disco vs. Web/`fetch`) para assets/audio/scripts; arranque del renderer como máquina de estados para no bloquear el hilo de JS del navegador (`Renderer::new_async`); nuevos crates `exporters/web-player` (jugador wasm genérico) y `exporters/web` (empaquetador); `aigs export --target web`. Verificado por compilación/CI y un export de Robot Rescue de punta a punta; **pendiente validación manual en un navegador real** (no disponible en este entorno). | [runtime/crates/aigs-runtime/src/source.rs](runtime/crates/aigs-runtime/src/source.rs), [exporters/web-player/](exporters/web-player/), [exporters/web/](exporters/web/) |
| 2026-07-06 | **M13 completado y validado — FASE 2 (Motor completo) CERRADA:** validación manual confirmada (dos ejecuciones reales con espera entre medias: autoguardado y `elapsed_since_save()` funcionan), READMEs de uso por ejemplo, CHANGELOG 0.2.0, versión del proyecto 0.1.0→0.2.0 en todos los manifiestos, y **release v0.2.0** publicado por el workflow de release: instaladores del editor + binarios del CLI para 3 SO. Cierra la Fase 2 (M7–M13). | [Release v0.2.0](https://github.com/Codigo-Free/ai-game-studio/releases/tag/v0.2.0), [CHANGELOG.md](CHANGELOG.md) |
| 2026-07-05 | **M13 (cambio de alcance): persistencia entre partidas + demo Tamagotchi.** A petición del usuario, reemplaza a la demo de plataformas original. `save.json` fuera del formato (estado de partida, no dato de diseño): `SaveData`, memoria de scripts persistida por id de entidad a través de `goto_scene`, `elapsed_since_save()` de consumo único, `set_scale()`. `aigs run` carga/autoguarda (cada ~10 s). Demo `examples/tamagotchi/`: mascota con stats que decaen en tiempo real (incluso con el juego cerrado), cuidada con teclas 1/2/3, un único script. Probado localmente en dos ejecuciones reales con una espera entre medias: el autoguardado y `elapsed_since_save()` funcionan como se diseñaron. | [runtime/crates/aigs-runtime/src/save.rs](runtime/crates/aigs-runtime/src/save.rs), [examples/tamagotchi/](examples/tamagotchi/) |
| 2026-07-03 | Redacción inicial de la documentación de diseño (11 documentos en `docs/`), sesión AI-First con Claude Code. | `docs/` |
| 2026-07-03 | Definición del plan maestro del MVP: hitos M0–M6, criterio de éxito, riesgos. | [docs/plan.md](docs/plan.md) |
| 2026-07-03 | Decisiones fundacionales: formato `.aigs` AI-Ready, ECS propio, WGPU, Tauri. | [docs/arquitectura.md](docs/arquitectura.md) |
| 2026-07-03 | Creación del monorepo (estructura de directorios, README, LICENSE MIT, CONTRIBUTING, ROADMAP, CLAUDE.md) y publicación en GitHub. | este repositorio |
| 2026-07-03 | **M0 completado:** workspace Cargo (`aigs-ecs`, `aigs-render`, `aigs-anim`, `aigs-project`, `aigs-cli`), especificación v0 del formato `.aigs`, ejemplo `hello-world` validado por el CLI, scaffold del editor Tauri 2 + React, CI multiplataforma en GitHub Actions. | commit `3683636`, [sdk/aigs-format/SPEC.md](sdk/aigs-format/SPEC.md) |
| 2026-07-03 | **M1 completado:** ECS con componentes/consultas/Schedule, renderer WGPU (batching instanciado, capas, cámara ortográfica), crate `aigs-runtime` (loop 60 Hz interpolado, input, runner winit), ejemplo `bouncing-sprites` verificado en GPU local. Decisiones nuevas en la tabla de arquitectura. | [docs/arquitectura.md](docs/arquitectura.md), [examples/bouncing-sprites/](examples/bouncing-sprites/) |
| 2026-07-03 | **M2 completado:** pipeline de assets (`AssetStore`), instanciación de escenas `.aigs` → `World`, comando `aigs run`, test de integración sobre `hello-world` real, sprite `width`/`height` en el formato. Corregido segfault de teardown Wayland/EGL. | [tests/integration/](tests/integration/), [sdk/aigs-format/SPEC.md](sdk/aigs-format/SPEC.md) |
| 2026-07-03 | **M3 completado y validado:** editor base — backend Tauri con guardado revalidado por `aigs-project`, documento con undo/redo, viewport Canvas 2D, árbol, inspector, recursos con importación, consola, Play → `aigs run`. Validación manual del usuario: abrir hello-world, mover entidad en el viewport, guardar y ejecutar con Play. | [editor/](editor/), [docs/editor.md](docs/editor.md) |
| 2026-07-04 | **Parche M12 (a petición del usuario):** manifiesto tipado de la API (`api_manifest()` + `scripting-api.json` + `aigs script-api`, con smoke test y test de consistencia), hot reload de scripts por mtime (~2×/s, sin reiniciar Play), eventos `on_collision`/`on_destroy`, y corrección de diseño: `this.campo` nunca persistía entre ticks en rhai (las `fn` no ven el Scope externo) — reemplazado por `get_var`/`set_var` explícitos. 27 tests en aigs-runtime tras el parche (4 nuevos). | [runtime/crates/aigs-runtime/src/scripting.rs](runtime/crates/aigs-runtime/src/scripting.rs), [sdk/aigs-format/scripting-api.json](sdk/aigs-format/scripting-api.json) |
| 2026-07-04 | **M12 completado y validado** (partida completa con el dron patrullero activo): scripting rhai — ScriptHost sandboxeado con API de snapshot+comandos, on_start/on_update con estado persistente, errores como warnings sin tumbar el juego, import .rhai y sección Script en el editor, dron patrullero en robot-rescue. 23 tests runtime. | [runtime/crates/aigs-runtime/src/scripting.rs](runtime/crates/aigs-runtime/src/scripting.rs) |
| 2026-07-04 | **M11 completado y validado** (playground a 60 FPS sostenidos): partículas — componente particles + acción emit_particles, sistema como entidades ECS (tick por acumulador, bursts, fade/shrink), 22 tests runtime, sección con preview en vivo en el inspector, chispas y confeti en las demos. | [runtime/crates/aigs-runtime/src/particles.rs](runtime/crates/aigs-runtime/src/particles.rs) |
| 2026-07-04 | **M10 completado y validado** (partida completa con el robot caminante): spritesheets con UV por instancia, sprite.frame animable, key_released, máquinas de estados de animación (animator) en formato+runtime+editor, robot caminante en robot-rescue. 18 tests runtime. Bezier diferido. | [sdk/aigs-format/SPEC.md](sdk/aigs-format/SPEC.md), [runtime/crates/aigs-runtime/src/player.rs](runtime/crates/aigs-runtime/src/player.rs) |
| 2026-07-04 | **M9 completado y validado** (partida completa menú→nivel→victoria con sonido): audio con kira — AudioPlayer (efectos + música por escena con continuidad entre escenas, no-op sin dispositivo), acción play_sound, música/pre-escucha en el editor, WAVs procedurales en las demos, ALSA en CI. | [runtime/crates/aigs-runtime/src/audio.rs](runtime/crates/aigs-runtime/src/audio.rs) |
| 2026-07-04 | **M8 completado y validado:** física 2D (rapier2d), componentes rigidbody2d/collider2d, gravedad por escena, evento collision con filtro, tests sin GPU (caída+reposo, colisión→escena, sensor), física en inspector del editor. Playground validado manualmente a 60 FPS sostenidos. | [runtime/crates/aigs-runtime/src/physics.rs](runtime/crates/aigs-runtime/src/physics.rs), [examples/physics-playground/](examples/physics-playground/) |
| 2026-07-03 | **Fase 2 planificada y M7 completado:** plan de Fase 2 (M7–M13) en docs/plan.md; exportador Desktop con diseño self-player (`aigs export`, crate `aigs-export-desktop`, botón Exportar en el editor). Robot Rescue exportado y verificado standalone (63 FPS en carpeta limpia). | [docs/plan.md](docs/plan.md), [exporters/desktop/](exporters/desktop/) |
| 2026-07-03 | **M6 completado — FASE 1 (MVP) CERRADA:** benchmarks Criterion (baseline en testing.md), demo Robot Rescue (menú+nivel+victoria, 100 % `.aigs`), CHANGELOG, guía de inicio rápido, y **release v0.1.0** publicado por el workflow de release: 7 instaladores del editor (Linux deb/rpm/AppImage, Windows msi/exe, macOS dmg/app) + 3 binarios del CLI. | [Release v0.1.0](https://github.com/Codigo-Free/ai-game-studio/releases/tag/v0.1.0), [CHANGELOG.md](CHANGELOG.md), [docs/guia-inicio.md](docs/guia-inicio.md) |
| 2026-07-03 | **M5 completado y validado:** behaviors sin código en el formato (evento→acción), `GamePlayer` multi-escena con `goto_scene` y hit-test de clic, `aigs run` con stats, gestión de escenas y comportamientos en el editor, logs del player en la consola del editor. hello-world convertido en juego menú+nivel. Validado: round-trip de behaviors por el guardado del editor y juego ejecutado. | [sdk/aigs-format/SPEC.md](sdk/aigs-format/SPEC.md), [runtime/crates/aigs-runtime/src/player.rs](runtime/crates/aigs-runtime/src/player.rs) |
| 2026-07-03 | **M4 completado y validado:** evaluación de keyframes (`aigs_anim::sample`), `AnimationPlayback` en runtime (loop/hold, warnings de bind), `aigs run` reproduce animaciones, panel Timeline en el editor (pistas, keyframes arrastrables, easing, scrubbing, play con preview en viewport), semántica de reproducción en la SPEC. Validación manual: keyframe arrastrado en el Timeline y guardado. Spritesheet diferido a Fase 2. | [sdk/aigs-format/SPEC.md](sdk/aigs-format/SPEC.md), [editor/src/panels/Timeline.tsx](editor/src/panels/Timeline.tsx) |
