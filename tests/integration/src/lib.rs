//! Integration tests: real `.aigs` project files from `examples/` loaded by
//! `aigs-project` and instantiated into a live world by `aigs-runtime`,
//! without touching the GPU (textures are resolved by a fake).
